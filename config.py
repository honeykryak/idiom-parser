"""
Центральная конфигурация — значения слова «лапша», имя модели, эталонные фразы.

IDIOM_MEANINGS хранит **прямое** и **переносное** значения слова «лапша».
Эталонные фразы кодируются семантической моделью и сравниваются
с каждым входным предложением, чтобы определить, употреблено ли слово
«лапша» буквально (еда) или в переносном смысле (обман / сегментация).
"""

# ── Описания значений ────────────────────────────────────────────────────
# Каждое значение — список коротких эталонных фраз, передающих смысл.
# Модель усредняет их эмбеддинги для построения устойчивого прототипа.

IDIOM_MEANINGS: dict[str, list[str]] = {
    # Переносное (идиоматическое / обманное) значение:
    #   «лапша» = обман, ложь, ввести в заблуждение
    "figurative": [
        "обман, ложь, вранье, надувательство",
        "обманывать, вводить в заблуждение, дурить",
        "неправда, дезинформация, пропаганда",
        "лапша на уши — значит обман",
        "врать, лгать, мошенничать, манипулировать",
        "ложная информация, фальшивка, подлог",
    ],

    # Прямое (буквальное / пищевое) значение:
    #   «лапша» = мучное изделие, еда, блюдо, суп
    "literal": [
        "еда, пища, блюдо из теста, гарнир",
        "макаронные изделия, лапша как продукт питания",
        "суп с лапшой, варить лапшу, готовить лапшу",
        "куриная лапша, китайская лапша, японская лапша",
        "Доширак, лапша быстрого приготовления, вермишель",
        "кастрюля с лапшой, порция лапши, тарелка лапши",
        "рецепт, кухня, обед, завтрак, ужин",
        "бульон, креветки, мясо, курица, овощи",
    ],
}

# ── Глаголы «вешать» и однокоренные ────────────────────────────────────────
# Слой 1: множество допустимых лемм (когда Natasha лемматизирует корректно)
HANG_VERB_LEMMAS: set[str] = {
    "вешать", "повесить", "навешать", "навесить",
    "навешивать", "развешивать", "развесить",
    "понавешать", "понавесить"
}

# Слой 2: основа для поиска в сырых токенах (когда лемматизация не сработала)
# Покрывает все формы с чередованием с/ш: вешать→веш, повесить→повеш→повеш*
HANG_VERB_STEM = "веш"

# Максимальное расстояние (в словах) между «лапша» и глаголом «вешать»
# (по умолчанию 7; можно переопределить через argv)
DEFAULT_MAX_DISTANCE = 7

# Максимальное расстояние между «лапша» и «ухо» для признания сегментации
UHO_MAX_DISTANCE = 5

# Максимальное расстояние между «лапша» и пищевым маркером для признания прямого значения
FOOD_MAX_DISTANCE = 5

# ── Пищевые маркеры ─────────────────────────────────────────────────
# Основы слов, указывающих на пищевой контекст рядом с «лапша».
# Используется как быстрый фильтр перед семантической моделью.
FOOD_STEMS: set[str] = {
    "курин",  # куриная, куриный
    "молочн",  # молочная
    "доширак",
    "быстрорастворим",
    "вермишел",
    "рисов",   # рисовая
    "гречнев",  # гречневая
    "удон",
    "соба",    # японская лапша
    "якисоба",
    "суп",
    "бульон",
    "кастрюл",
    "тарелк",
    "порци",   # порция, порцию
    "варить", "сварить", "отварить",
    "готовить", "приготовить",
    "жарен",   # жареная, жарить
    "кипят",   # кипяток, кипятить
    "корейск",  # корейская
    "японск",  # японская
    "китайск",  # китайская
    "домашн",  # домашняя
    "молоканск",  # молоканская
    "питаться", "питал",
    "кушать",
    "макарон",
    "реклам",  # реклама лапши = пищевой контекст
}

# ── Семантическая модель ──────────────────────────────────────────────────
MODEL_NAME = "cointegrated/rubert-tiny2"

# Папка для кэширования модели (модель скачивается один раз, затем используется локально)
import os as _os
MODEL_CACHE_DIR = _os.path.join(_os.path.dirname(_os.path.abspath(__file__)), "model_cache")

# Если HuggingFace заблокирован, установите
# переменную окружения перед первым запуском:
#   $env:HF_ENDPOINT = "https://hf-mirror.com"          # PowerShell
#   export HF_ENDPOINT="https://hf-mirror.com"           # bash
